# Shared artifacts

Place content reused by multiple charters, spec dimensions, or plans **at this level only**.

- Do not copy full text into `spec.md` / `plan.md` — link here.
- Register paths in the nearest `*-manifest.yml` under `includes.module` or `includes.charter`.
