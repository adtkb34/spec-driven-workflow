# Cursor User Rules 片段（可选粘贴）

工作流操作者可在 Cursor User Rules 追加：

```
本仓库使用 spec-driven-workflow。特性路径从 .specify/feature.json 解析。
每阶段先 phase-brief.sh --phase <阶段>；full ceremony 等我说「继续」再读 skill 全文。
run-log --skills 只记实际 Read 的 skill。回复语言：简体中文。
```

全局偏好默认 ignore（见 stack.yml global_prefs），不覆盖 constitution / gate。
