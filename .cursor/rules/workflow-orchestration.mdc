---
alwaysApply: true
---

# 工作流编排规则（Spec-Driven Orchestration · 薄壳）

与 `.specify/memory/constitution.md`、`.cursor/registry.yaml`、`.specify/workflows/workflow-index.yml` 配合。
完整设计见 `ARCHITECTURE.html`。**阶段提问驱动**见 `.cursor/rules/phase-discipline.mdc`（P0，alwaysApply）。

## 阶段入口顺序（P0 → P2 → D1）

1. **P0**：`phase-brief.sh`（只带提问，见 `phase-index.yml`）
2. **P1**：对应 `speckit-*` SKILL.md（full ceremony 时等用户「继续」再 Read）
3. **P2**：gate 脚本 + `phase.yml` 状态；`--unlock-status` **实时**查 gate，**不**在 phase.yml 存 unlock 布尔
4. **P2 通过后** → `activate-dimensions.sh` → 注入 registry **D1 summary**（非 P2 前全文加载）

命名：**P0–P2** = 阶段树；**D1–D3** = registry 渐进披露（summary / SKILL.md / references/）。

## 章节索引（按需 Read，不必一次加载）

| 主题 | 路径 |
|------|------|
| 维度披露 + 阶段开始 | `.specify/memory/orchestration/dimension-disclosure.md` |
| 增强层 + 环境隔离 | `.specify/memory/orchestration/enhancement-layer.md` |
| 复杂度 / trivial 通道 | `.specify/memory/orchestration/triage-fast-track.md` |
| Gates + analyze checklist | `.specify/memory/orchestration/gates.md` |
| 技术栈闸门 | `.specify/memory/orchestration/stack-gate.md` |
| Run Log | `.specify/memory/orchestration/run-log.md` |
| verify / deliver / release | `.specify/memory/orchestration/verify-deliver-release.md` |
| amend / handoff | `.specify/memory/orchestration/iteration-handoff.md` |

各 speckit 阶段在 `workflow-index.yml` 中映射 `orchestration_chapters` — 进入该阶段时 Read 对应章节即可。

## 模型路由（算力档建议 · 与 run-log 分离）

| 算力档 | 阶段 | Cursor 典型 | Copilot 典型 |
|--------|------|-------------|--------------|
| **strong** | specify/clarify/plan/analyze/tasks/verify/release | Opus 系 | GPT-4.1 / o 系列 |
| **fast** | implement / deliver 脚本执行 | Composer 系 | 较快档 |
| **strong↑** | implement 卡住 / verify 裁决 / release go-no-go | 临时升 Opus | 换更强档 |

**run-log `--model`** 只记实际模型 ID，不写 Opus/Composer 档位名。细节见 constitution「模型路由」表。

## 硬约束摘要

- 阶段结束未写 run-log → 不得进下一阶段
- `--skills` 只记实际 Read/执行的 skill（见 run-log.md）
- 进 plan 前：gate-clarify + 技术栈闸门（stack.confirmed）
- implement 后必 verify；上线仅用户触发 release
- 外科式变更：implement 只改与任务直接相关的代码
